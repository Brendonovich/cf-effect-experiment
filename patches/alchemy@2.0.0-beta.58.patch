diff --git a/lib/Plan.js b/lib/Plan.js
index b4390e4ba88b69b2b178bdb3b5af142460bbb39d..8ce44f71fabf4d52f8023f3bfa57dd5c7f1b79fd 100644
--- a/lib/Plan.js
+++ b/lib/Plan.js
@@ -134,7 +134,7 @@ Effect.gen(function* () {
                 }
             })));
     });
-    const resolveInput = (input) => Effect.gen(function* () {
+    const resolveInput = (input, inputPath = "$") => Effect.gen(function* () {
         if (!input) {
             return input;
         }
@@ -148,7 +148,7 @@ Effect.gen(function* () {
             // providers receive a resolved value instead of a Config object.
             // `Config.redacted` resolves to a `Redacted`, which stays opaque via
             // the branch below.
-            return yield* resolveInput(yield* input);
+            return yield* resolveInput(yield* input, inputPath);
         }
         else if (Duration.isDuration(input) || Redacted.isRedacted(input)) {
             // Opaque values that are resolved downstream. We don't walk them
@@ -158,7 +158,7 @@ Effect.gen(function* () {
             return input;
         }
         else if (Array.isArray(input)) {
-            return yield* Effect.all(input.map(resolveInput), {
+            return yield* Effect.all(input.map((value, index) => resolveInput(value, `${inputPath}[${index}]`)), {
                 concurrency: "unbounded",
             });
         }
@@ -179,12 +179,17 @@ Effect.gen(function* () {
             // short-circuits, and a stable identifier that should have been
             // available is missing — forcing consumers to hand-extract it.
             if (Output.isResourceExpr(resolved) && resolved.stables) {
-                return yield* resolveInput(resolved.stables);
+                return yield* resolveInput(resolved.stables, inputPath);
             }
-            return yield* resolveInput(resolved);
+            return yield* resolveInput(resolved, inputPath);
+        }
+        else if (!inputPath.endsWith(".constructor") &&
+            typeof input !== "function" &&
+            Effect.isEffect(input)) {
+            return yield* resolveInput(yield* input, inputPath);
         }
         else if (typeof input === "object") {
-            return Object.fromEntries(yield* Effect.all(Object.entries(input).map(([key, value]) => resolveInput(value).pipe(Effect.map((value) => [key, value]))), { concurrency: "unbounded" }));
+            return Object.fromEntries(yield* Effect.all(Object.entries(input).map(([key, value]) => resolveInput(value, `${inputPath}.${key}`).pipe(Effect.map((value) => [key, value]))), { concurrency: "unbounded" }));
         }
         return input;
     });
diff --git a/src/Plan.ts b/src/Plan.ts
index d9acb691d808710740faccfc51c25cd887b8e810..092f4ea95e6a4ae845710bca0547751cd661cafa 100644
--- a/src/Plan.ts
+++ b/src/Plan.ts
@@ -375,7 +375,10 @@ export const make = <A>(
           ));
       });
 
-    const resolveInput = (input: any): Effect.Effect<any, Config.ConfigError> =>
+    const resolveInput = (
+      input: any,
+      inputPath = "$",
+    ): Effect.Effect<any, Config.ConfigError> =>
       Effect.gen(function* () {
         if (!input) {
           return input;
@@ -388,7 +391,7 @@ export const make = <A>(
           // providers receive a resolved value instead of a Config object.
           // `Config.redacted` resolves to a `Redacted`, which stays opaque via
           // the branch below.
-          return yield* resolveInput(yield* input);
+          return yield* resolveInput(yield* input, inputPath);
         } else if (Duration.isDuration(input) || Redacted.isRedacted(input)) {
           // Opaque values that are resolved downstream. We don't walk them
           // because it would strip their prototype, resulting in a plain object
@@ -396,9 +399,12 @@ export const make = <A>(
           // stays wrapped to preserve the secrecy boundary.
           return input;
         } else if (Array.isArray(input)) {
-          return yield* Effect.all(input.map(resolveInput), {
-            concurrency: "unbounded",
-          });
+          return yield* Effect.all(
+            input.map((value, index) => resolveInput(value, `${inputPath}[${index}]`)),
+            {
+              concurrency: "unbounded",
+            },
+          );
         } else if (isResource(input)) {
           // Resource objects have dynamic properties (path, hash, etc.) that are
           // created on-demand by a Proxy getter and aren't enumerable via Object.entries.
@@ -416,14 +422,22 @@ export const make = <A>(
           // short-circuits, and a stable identifier that should have been
           // available is missing — forcing consumers to hand-extract it.
           if (Output.isResourceExpr(resolved) && resolved.stables) {
-            return yield* resolveInput(resolved.stables);
+            return yield* resolveInput(resolved.stables, inputPath);
           }
-          return yield* resolveInput(resolved);
+          return yield* resolveInput(resolved, inputPath);
+        } else if (
+          !inputPath.endsWith(".constructor") &&
+          typeof input !== "function" &&
+          Effect.isEffect(input)
+        ) {
+          return yield* resolveInput(yield* input, inputPath);
         } else if (typeof input === "object") {
           return Object.fromEntries(
             yield* Effect.all(
               Object.entries(input).map(([key, value]) =>
-                resolveInput(value).pipe(Effect.map((value) => [key, value])),
+                resolveInput(value, `${inputPath}.${key}`).pipe(
+                  Effect.map((value) => [key, value]),
+                ),
               ),
               { concurrency: "unbounded" },
             ),

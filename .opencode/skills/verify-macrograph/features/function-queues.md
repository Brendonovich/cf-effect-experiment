# Local function queues

The `function-queues` mode opens queue navigation, creates and opens a local typed queue canvas,
adds a signature argument, and verifies its live active, paused, and resumed states through the
production editor RPC path.

Evidence includes a screenshot and the playground server log.

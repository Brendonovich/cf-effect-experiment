# Local function queues

The `function-queues` mode creates a function, opens the dedicated Queues workspace pane, creates a
local queue that references that function, and verifies its live active, paused, and resumed states
through the production editor RPC path.

Evidence includes a screenshot and the playground server log.
